

```mermaid
graph TD
    A[เริ่ม] --> B(ผู้ใช้เข้าสู่ระบบ);
    B -- ตรวจสอบรหัสผ่าน --> C{รหัสผ่านถูกต้อง?};
    C -- ใช่ --> D[แสดงหน้าสินค้า];
    C -- ไม่ใช่ --> B;
    D --> E[เลือกสินค้าและใส่ตะกร้า];
    E --> F[ดำเนินการชำระเงิน];
    F --> G[เสร็จสิ้น];
